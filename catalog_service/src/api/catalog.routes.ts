import express from 'express'
const router = express.Router()

// router.get('/')

// router.post('/')

// router.patch('/:id')

// router.get('/:id')

// router.delete('/:id')

export { router as catalogRoutes }
